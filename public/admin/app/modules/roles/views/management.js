/* global app*/
define([
    'tpl!modules/roles/templates/management.html'
], function (Template) {

    'use strict';

    return Marionette.ItemView.extend({
        template: Template,
        events: {},
        initialize: function () {
        },
        onRender: function () {
        }
    });
});
