/* global app*/
define([
    'tpl!modules/roles/templates/register.html'
], function (Template) {

    'use strict';

    return Marionette.ItemView.extend({
        template: Template,
        events: {},
        initialize: function () {
        },
        onRender: function () {
        }
    });
});
