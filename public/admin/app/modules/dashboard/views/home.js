/* global app*/
define([
    'tpl!modules/dashboard/templates/home.html'
], function (Template) {

    'use strict';

    return Marionette.ItemView.extend({
        template: Template,
        events: {},
        initialize: function () {
        },
        onRender: function () {
        }
    });
});
